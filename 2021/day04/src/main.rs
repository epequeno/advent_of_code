fn main() {
    println!("Hello, world!");
    // parse drawings
    // parse into board objects
        // boards are made of cells which hold value and an is_called boolean
    // on each step, check each boards rows and cols to see if all are is_called
    // if row or col is identified, sum all the _unmarked_ numbers * last called num
}
